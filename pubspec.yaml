name: fitness_tracker_app
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: '>=3.3.0-279.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  smooth_page_indicator: ^1.1.0
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  http:
  calendar_slider: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/onboardingImages/
    - assets/images/
    - assets/images/meee.jpg
  